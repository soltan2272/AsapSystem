import { NodeCompatibleEventEmitter } from "rxjs/internal/observable/fromEvent";

export interface IPerson {
    id:number;
    first_Name:string;
    last_Name:string;
    email:string;
    phone:number;
}
